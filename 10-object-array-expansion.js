// Object expansion
let car = {
    brand: 'Toyota',
    model: 2012
};
console.log(car);

function process(car) {
    // let carWithColor =
    // printColoredCar(carWithColor)
}

function printColoredCar(car) {
    console.log(car);
}

process(car);

// array expansion
